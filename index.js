import { AppRegistry } from 'react-native';
import App from './App'; // Ensure this points to your App.tsx file

// Register the App component to the 'main' entry point
AppRegistry.registerComponent('main', () => App);
